<!-- 首页仪表盘 -->
<t-navbar title="断联计划" fixed placeholder></t-navbar>
<scroll-view type="custom" scroll-y class="page-scroll">
  <view class="page-container">

    <!-- 数据概览卡片 -->
    <view class="stats-card">
      <view class="stats-bg"></view>
      <view class="stats-content">
        <view class="stats-main">
          <text class="stats-number">127</text>
          <text class="stats-unit">天</text>
        </view>
        <text class="stats-label">累计断联时长</text>
        <view class="stats-row">
          <view class="stats-item">
            <text class="stats-value">2</text>
            <text class="stats-desc">进行中</text>
          </view>
          <view class="stats-divider"></view>
          <view class="stats-item">
            <text class="stats-value">1</text>
            <text class="stats-desc">已结束</text>
          </view>
          <view class="stats-divider"></view>
          <view class="stats-item">
            <text class="stats-value">15</text>
            <text class="stats-desc">总记录</text>
          </view>
        </view>
      </view>
    </view>

    <!-- 进行中的断联 -->
    <view class="section-header flex-between">
      <text class="section-title">进行中的断联</text>
    </view>

    <!-- 断联卡片列表 -->
    <view
      class="relation-card"
      wx:for="{{list}}"
      wx:key="id"
      data-id="{{item.id}}"
      bindtap="onTapCard"
    >
      <view class="card-header flex-between">
        <view class="flex-row">
          <view class="avatar avatar-{{item.avatarColor}}">
            <t-icon name="user" size="48rpx" color="#fff" />
          </view>
          <view class="card-header-info">
            <text class="card-title">{{item.name}}</text>
            <text class="card-subtitle">{{item.subtitle}}</text>
          </view>
        </view>
        <view class="tag tag-{{item.status === '已达成' ? 'success' : 'warning'}}">{{item.status}}</view>
      </view>
      <view class="card-body">
        <view class="flex-between mb-16">
          <text class="text-secondary">已断联 {{item.days}} 天</text>
          <text class="text-hint">最近记录：{{item.lastRecord}}</text>
        </view>
        <view class="progress-wrap">
          <t-progress percentage="{{item.progress}}" stroke-width="12rpx" color="#f6685d" track-color="#fde8e5" />
          <text class="progress-hint">目标 {{item.targetDays}} 天 · 还剩 {{item.targetDays - item.days}} 天</text>
        </view>
      </view>
    </view>

  </view>
</scroll-view>

<!-- 悬浮新增按钮（置于 scroll-view 外以保持 fixed 定位） -->
<view class="fab-btn" bindtap="onAddRelationship">
  <t-icon name="add" size="48rpx" color="#fff" />
</view>
